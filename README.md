# CS846_Project
This is a project directory for CS 846 course.

## Deep Pagination Linit in `download_crashes.py`

Socorro limits SuperSearch to 10,000 results per query window. If a single day has more than 10,000 crashes, use `--by-hour` to split into smaller windows.